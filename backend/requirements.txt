# ── Core text extraction ──────────────────────────────────────
pdfminer.six==20221105
python-docx==1.1.2

# ── NLP ───────────────────────────────────────────────────────
# spaCy 3.8.x works cleanly on Python 3.11
# en_core_web_sm is downloaded separately (see README)
spacy==3.8.4

# ── ML scoring ───────────────────────────────────────────────
numpy==1.26.4

# ── AWS SDK — must match SAM CLI 1.156.0 requirement ─────────
boto3==1.42.70

# ── HTTP — must match SAM CLI 1.156.0 requirement ────────────
requests==2.32.5